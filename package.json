{
  "dependencies": {
    "sass": "^1.82.0"
  },
  "engines": {
    "node": "22.12.0"
  },
  "scripts": {
    "build": "npm run compile-sass && npm run other-build-tasks",
    "compile-sass": "sass src/styles:dist/styles"
  }
}
